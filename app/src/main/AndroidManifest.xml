<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" />

    <application
        android:name=".ZuParkApplication"
        android:allowBackup="true"
        android:icon="@drawable/zupark_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/zupark_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParkingLKS">
        <activity
            android:name=".view.activity.RegisterActivity"
            android:exported="true" >

        </activity>
        <activity
            android:name=".view.activity.LoginActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".view.activity.MainActivity"
            android:exported="true" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".view.activity.AjustesActivity"
            android:exported="true" >

        </activity>
        <receiver android:name=".util.ReservaNotificationUtil$ReservaNotificationReceiver" android:exported="false" />
    </application>

</manifest>